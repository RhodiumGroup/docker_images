metadata:
spec:
  restartPolicy: Never
  containers:
  - args:
      - dask-worker
      - --nthreads
      - '1'
      - --no-bokeh
      - --memory-limit
      - 11.5GB
      - --death-timeout
      - '60'
    env:
      - name: GCSFUSE_BUCKET
        value: rhg-data
    image: rhodium/worker:dev-update-packages
    name: dask-worker
    securityContext:
      capabilities:
        add: [SYS_ADMIN]
      privileged: true
    resources:
      limits:
        cpu: "1.75"
        memory: 11.5G
      requests:
        cpu: "1.75"
        memory: 11.5G
